# Julia artifacts
Manifest.toml
*.jl.cov
*.jl.mem
*.jl.mem.*

# OS/Editor junk
.DS_Store
.vscode/
.history/

# Documentation and paper artifacts
docs/build/
paper/*.aux
paper/*.log
paper/*.out
paper/*.toc
paper/*.synctex.gz
paper/*.spl
paper/*.bbl
paper/*.blg
paper/*.fls
paper/*.fdb_latexmk
paper/*.lof
paper/*.lot

# Paper pipeline: large cached grids are regenerable, do not commit
paper/data/cache/
# low-resolution previews written next to the real figures for quick inspection
paper/figures/preview/

# Paper pipeline: keep generated figures and the pinned manifest despite global ignores
!paper/figures/*.png
!paper/figures/*.pdf
!paper/scripts/Manifest.toml

*.png
*.svg

# Output figures (usually generated by user)
output_figures/
Manifest.toml
